# Share Screen Configuration
# Copy this file to .env and uncomment/modify values as needed

# Server Configuration
# ===================

# Server port (default: 8080 for HTTP, 8443 for HTTPS)
PORT=8080

# Enable HTTPS (default: false)
# Set to 'true' to enable HTTPS mode
ENABLE_HTTPS=false

# TLS Certificate Configuration (required when ENABLE_HTTPS=true)
# ==============================================================

# Path to TLS certificate file (default: certs/server.crt)
TLS_CERT_FILE=certs/server.crt

# Path to TLS private key file (default: certs/server.key)
TLS_KEY_FILE=certs/server.key

# WebRTC Configuration
# ===================

# STUN server for WebRTC NAT traversal (default: Google STUN)
STUN_SERVER=stun:stun.l.google.com:19302

# Token expiry duration (default: 30m)
# Examples: 15m, 1h, 2h30m
TOKEN_EXPIRY=30m

# Docker Configuration
# ===================

# HTTP port mapping for Docker (default: 8080)
HTTP_PORT=8080

# HTTPS port mapping for Docker (default: 8443)
HTTPS_PORT=8443

# Alternative STUN servers (choose one)
# ====================================
# STUN_SERVER=stun:stun1.l.google.com:19302
# STUN_SERVER=stun:stun2.l.google.com:19302
# STUN_SERVER=stun:stun.stunprotocol.org:3478
# STUN_SERVER=stun:stun.stunprotocol.org:3478

# Production HTTPS Example
# ========================
# Uncomment and modify for HTTPS in production:
# ENABLE_HTTPS=true
# PORT=8443
# TLS_CERT_FILE=/etc/ssl/certs/your-domain.crt
# TLS_KEY_FILE=/etc/ssl/private/your-domain.key